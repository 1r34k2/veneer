import { fetchRedis } from "./redis"

//function to get all users from redis
export async function getAllUsers(session){
    // const users = [] = await fetchRedis("scan", 0)
    // const people = []
    // users[1].forEach(user => {
    //     if(!user.includes("userId")){
    //         people.push(user)
    //     }
    // })
    // const cards = await Promise.all(
    //     people.map(async (personId) => {
    //         const person = await fetchRedis('get', `${personId}`)
    //         return person
    //     })
    // )
    // return cards
}